name: Ping

on:
  push:            # n'importe quel push, n'importe quelle branche
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Show branch and time
        run: |
          echo "✅ Workflow triggered."
          echo "Branch: $GITHUB_REF"
          date
